import { ArrowRight, BriefcaseBusiness, UserRound } from 'lucide-react';
import { useState } from 'react';
import { Link, useLocation, useNavigate, useSearchParams } from 'react-router-dom';
import { Button } from '../../shared/components/Button';
import { Input, Select } from '../../shared/components/FormControls';
import { PublicBrand } from '../../shared/layout/PublicLayout';

export function AuthPage() {
  const location = useLocation(); const navigate = useNavigate(); const [params] = useSearchParams(); const register = location.pathname === '/register'; const [role, setRole] = useState<'customer' | 'provider'>(params.get('role') === 'provider' ? 'provider' : 'customer'); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [submitted, setSubmitted] = useState(false); const [saving, setSaving] = useState(false);
  const submit = (event: React.FormEvent) => { event.preventDefault(); setSubmitted(true); if ((register && !name.trim()) || !email.includes('@') || password.length < 6) return; setSaving(true); window.setTimeout(() => navigate(role === 'provider' ? '/dashboard/provider' : '/user'), 500); };
  return <main className="grid min-h-[100dvh] bg-page lg:grid-cols-2"><section className="flex items-center justify-center px-4 py-8 sm:px-8"><div className="w-full max-w-md"><PublicBrand/><div className="mt-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-primary">{register ? 'Join the neighborhood' : 'Welcome back'}</p><h1 className="mt-3 text-3xl font-bold text-heading sm:text-4xl">{register ? 'Create your Jisr account' : 'Log in to Jisr'}</h1><p className="mt-2 text-sm leading-6 text-muted">{register ? 'Choose how you want to take part in your local marketplace.' : 'Continue to your customer or provider experience.'}</p></div>{register && <div className="mt-7 grid grid-cols-2 gap-3"><button aria-pressed={role === 'customer'} onClick={() => setRole('customer')} className={`min-h-24 rounded-xl border p-4 text-start ${role === 'customer' ? 'border-primary bg-primary-soft' : 'border-border bg-surface'}`}><UserRound className="h-5 w-5 text-primary"/><span className="mt-2 block text-sm font-semibold text-heading">Customer</span></button><button aria-pressed={role === 'provider'} onClick={() => setRole('provider')} className={`min-h-24 rounded-xl border p-4 text-start ${role === 'provider' ? 'border-primary bg-primary-soft' : 'border-border bg-surface'}`}><BriefcaseBusiness className="h-5 w-5 text-primary"/><span className="mt-2 block text-sm font-semibold text-heading">Provider</span></button></div>}<form noValidate onSubmit={submit} className="mt-7 space-y-5">{register && <Input label={role === 'provider' ? 'Your name or business name' : 'Full name'} required value={name} onChange={(event) => setName(event.target.value)} error={submitted && !name.trim() ? 'Enter your name.' : undefined}/>}<Input label="Email address" type="email" required value={email} onChange={(event) => setEmail(event.target.value)} error={submitted && !email.includes('@') ? 'Enter a valid email address.' : undefined}/><Input label="Password" type="password" required value={password} onChange={(event) => setPassword(event.target.value)} error={submitted && password.length < 6 ? 'Password must be at least 6 characters.' : undefined}/>{!register && <Select label="Continue as" value={role} onChange={(event) => setRole(event.target.value as 'customer' | 'provider')}><option value="customer">Customer</option><option value="provider">Provider</option></Select>}<Button type="submit" loading={saving} className="w-full" icon={<ArrowRight className="h-4 w-4"/>}>{register ? 'Create demo account' : 'Log in'}</Button></form><p className="mt-6 text-center text-sm text-muted">{register ? 'Already have an account?' : 'New to Jisr?'} <Link className="font-semibold text-primary hover:text-primary-hover" to={register ? '/login' : '/register'}>{register ? 'Log in' : 'Create an account'}</Link></p><p className="mt-4 text-center text-xs text-muted">Demo only—no credentials are stored or transmitted.</p></div></section><aside className="relative hidden overflow-hidden bg-sage-soft lg:block"><img src="https://images.unsplash.com/photo-1529156069898-49953e39b3ac?auto=format&fit=crop&w=1200&q=85" alt="Neighbors sharing time together" className="h-full w-full object-cover"/><div className="absolute inset-0 bg-heading/25"/><div className="absolute bottom-10 start-10 end-10 rounded-2xl bg-surface/95 p-6"><p className="text-xl font-semibold text-heading">People make places feel like home.</p><p className="mt-2 text-sm text-muted">Connect with trusted skills, food, and help in your neighborhood.</p></div></aside></main>;
}
